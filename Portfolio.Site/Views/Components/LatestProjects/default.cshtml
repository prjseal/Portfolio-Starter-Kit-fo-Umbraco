@inherits UmbracoViewPage<IEnumerable<ContentModels.Project>>
@using Portfolio.Core.Extensions

@if (Model != null && Model.Any())
{
    <section class="portfolio-block photography">
        <div class="container">
            <div class="row no-gutters">
                @foreach (var project in Model)
                {
                    <div class="col-md-6 col-lg-4 item zoom-on-hover">
                        <a href="@project.Url()" title="@project.GetPageTitle()">
                            <img class="img-fluid image" width="663" height="442" src="@project.MainImage.GetCropUrl("Project Preview Medium")" alt="@project.MainImage.GetAltText()">
                        </a>
                    </div>
                }
            </div>
        </div>
    </section>
}